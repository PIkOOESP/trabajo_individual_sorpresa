services:
  api:
    build: .
    container_name: pets-api
    environment:
      SPRING_PROFILES_ACTIVE: local
    ports:
      - "8080:8080"

  db:
      image: mysql:8.0
      restart: always
      environment:
        MYSQL_ROOT_PASSWORD: root
      ports:
        - "3306:3306"
      volumes:
        - ./sql/sql_mysql.sql:/docker-entrypoint-initdb.d/sql_mysql.sql
  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    restart: always
    ports:
      - "8085:80"
    environment:
      PMA_HOST: db
      PMA_PORT: 3306
    depends_on:
      - db